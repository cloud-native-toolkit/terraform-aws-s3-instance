id: github.com/cloud-native-toolkit/terraform-aws-s3-instance
name: aws-s3-instance
type: terraform
description: Module to provision s3-instance on aws
tags:
  - aws
  - s3 bucket
  - s3
  - instance s3
versions: [{platforms: [], providers: [{name: aws, source: "hashicorp/aws"}], dependencies: [], variables: [{name: bucket_prefix, type: string, description: (required since we are not using 'bucket') Creates a unique bucket name beginning with the specified prefix. Conflicts with bucket., default: s3-bucket, optional: true}, {name: acl, type: string, description: (Optional) The canned ACL to apply. Defaults to private. Conflicts with grant., default: private, optional: true}, {name: tags, type: map, description: (Optional) A mapping of tags to assign to the bucket., default: '{ environment = swew terraform = true }', optional: true}, {name: region, type: string, description: 'Please set the region where the resouces to be created ', default: ap-south-1, optional: true}, {name: access_key, type: string, default: "", optional: true}, {name: secret_key, type: string, default: "", optional: true}], version: v1.1.1, outputs: [{name: s3_bucket_id, description: The name of the bucket.}, {name: s3_bucket_arn, description: 'The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.'}, {name: s3_bucket_bucket_domain_name, description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.}, {name: s3_bucket_bucket_regional_domain_name, description: 'The bucket region-specific domain name. The bucket domain name including the region name, please refer here for format. Note: The AWS CloudFront allows specifying S3 region-specific endpoint when creating S3 origin, it will prevent redirect issues from CloudFront to S3 Origin URL.'}, {name: s3_bucket_hosted_zone_id, description: The Route 53 Hosted Zone ID for this bucket's region.}, {name: s3_bucket_region, description: The AWS region this bucket resides in.}]}, {platforms: [], dependencies: [], variables: [{name: bucket_prefix, type: string, description: (required since we are not using 'bucket') Creates a unique bucket name beginning with the specified prefix. Conflicts with bucket., default: s3-bucket, optional: true}, {name: acl, type: string, description: (Optional) The canned ACL to apply. Defaults to private. Conflicts with grant., default: private, optional: true}, {name: tags, type: map, description: (Optional) A mapping of tags to assign to the bucket., default: '{ environment = swew terraform = true }', optional: true}, {name: region, type: string, description: 'Please set the region where the resouces to be created ', default: ap-south-1, optional: true}, {name: access_key, type: string, default: "", optional: true}, {name: secret_key, type: string, default: "", optional: true}], version: v1.1.0, outputs: [{name: s3_bucket_id, description: The name of the bucket.}, {name: s3_bucket_arn, description: 'The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.'}, {name: s3_bucket_bucket_domain_name, description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.}, {name: s3_bucket_bucket_regional_domain_name, description: 'The bucket region-specific domain name. The bucket domain name including the region name, please refer here for format. Note: The AWS CloudFront allows specifying S3 region-specific endpoint when creating S3 origin, it will prevent redirect issues from CloudFront to S3 Origin URL.'}, {name: s3_bucket_hosted_zone_id, description: The Route 53 Hosted Zone ID for this bucket's region.}, {name: s3_bucket_region, description: The AWS region this bucket resides in.}]}, {platforms: [], dependencies: [], variables: [{name: bucket_prefix, type: string, description: (required since we are not using 'bucket') Creates a unique bucket name beginning with the specified prefix. Conflicts with bucket., default: s3-bucket, optional: true}, {name: acl, type: string, description: (Optional) The canned ACL to apply. Defaults to private. Conflicts with grant., default: private, optional: true}, {name: tags, type: map, description: (Optional) A mapping of tags to assign to the bucket., default: '{ environment = swew terraform = true }', optional: true}, {name: region, type: string, description: 'Please set the region where the resouces to be created ', default: ap-south-1, optional: true}, {name: access_key, type: string, default: "", optional: true}, {name: secret_key, type: string, default: "", optional: true}], version: v1.0.0, outputs: [{name: s3_bucket_id, description: The name of the bucket.}, {name: s3_bucket_arn, description: 'The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.'}, {name: s3_bucket_bucket_domain_name, description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.}, {name: s3_bucket_bucket_regional_domain_name, description: 'The bucket region-specific domain name. The bucket domain name including the region name, please refer here for format. Note: The AWS CloudFront allows specifying S3 region-specific endpoint when creating S3 origin, it will prevent redirect issues from CloudFront to S3 Origin URL.'}, {name: s3_bucket_hosted_zone_id, description: The Route 53 Hosted Zone ID for this bucket's region.}, {name: s3_bucket_region, description: The AWS region this bucket resides in.}]}, {platforms: [], dependencies: [], variables: [{name: bucket_prefix, type: string, description: (required since we are not using 'bucket') Creates a unique bucket name beginning with the specified prefix. Conflicts with bucket., default: s3-bucket, optional: true}, {name: acl, type: string, description: (Optional) The canned ACL to apply. Defaults to private. Conflicts with grant., default: private, optional: true}, {name: tags, type: map, description: (Optional) A mapping of tags to assign to the bucket., default: '{ environment = swew terraform = true }', optional: true}, {name: region, type: string, description: 'Please set the region where the resouces to be created ', default: ap-south-1, optional: true}, {name: access_key, type: string, default: "", optional: true}, {name: secret_key, type: string, default: "", optional: true}], version: v0.0.2, outputs: [{name: s3_bucket_id, description: The name of the bucket.}, {name: s3_bucket_arn, description: 'The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.'}, {name: s3_bucket_bucket_domain_name, description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.}, {name: s3_bucket_bucket_regional_domain_name, description: 'The bucket region-specific domain name. The bucket domain name including the region name, please refer here for format. Note: The AWS CloudFront allows specifying S3 region-specific endpoint when creating S3 origin, it will prevent redirect issues from CloudFront to S3 Origin URL.'}, {name: s3_bucket_hosted_zone_id, description: The Route 53 Hosted Zone ID for this bucket's region.}, {name: s3_bucket_region, description: The AWS region this bucket resides in.}]}, {platforms: [], dependencies: [], variables: [{name: bucket_prefix, type: string, description: (required since we are not using 'bucket') Creates a unique bucket name beginning with the specified prefix. Conflicts with bucket., default: s3-bucket, optional: true}, {name: acl, type: string, description: (Optional) The canned ACL to apply. Defaults to private. Conflicts with grant., default: private, optional: true}, {name: tags, type: map, description: (Optional) A mapping of tags to assign to the bucket., default: '{ environment = swew terraform = true }', optional: true}, {name: region, type: string, description: 'Please set the region where the resouces to be created ', default: ap-south-1, optional: true}, {name: access_key, type: string, default: "", optional: true}, {name: secret_key, type: string, default: "", optional: true}], version: v0.0.1, outputs: [{name: s3_bucket_id, description: The name of the bucket.}, {name: s3_bucket_arn, description: 'The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.'}, {name: s3_bucket_bucket_domain_name, description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.}, {name: s3_bucket_bucket_regional_domain_name, description: 'The bucket region-specific domain name. The bucket domain name including the region name, please refer here for format. Note: The AWS CloudFront allows specifying S3 region-specific endpoint when creating S3 origin, it will prevent redirect issues from CloudFront to S3 Origin URL.'}, {name: s3_bucket_hosted_zone_id, description: The Route 53 Hosted Zone ID for this bucket's region.}, {name: s3_bucket_region, description: The AWS region this bucket resides in.}]}]
cloudProvider: aws
